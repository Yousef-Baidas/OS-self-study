# OS-self-study — Rework (2026-07-17)

Turn dense textbook prose into scannable **lecture notes with real slide citations**, elevate the
look/feel to shadcn-caliber on the existing lean Astro+Svelte+tokens stack, and build a working
**exam mode**. Chapter 1 is the reusable template; Ch2–10 follow later. **No GSD.**

## Workstreams

### A · Slide citations (foundation)
- `SlideRef.astro` — quiet inline pill: `slide 16`, `slides 29–32`. Reference-only (deck not hosted;
  no LibreOffice to rasterize slides). Accessible (`title`, aria-label).
- Section H2s carry a slide-range badge; specific claims get inline refs.
- Citations mapped from `CONTENT/slides/OS_chapter1.pptx` (39 slides). Verified per-slide text
  extraction works. Section→slide map:
  - What is an OS? → S3, S5–S11
  - Computer System Organization → S12
  - Computer Startup → S13–S15
  - Storage-Device Hierarchy → S16–S20
  - Multiprocessing & Clustered → S21–S25
  - Multiprogramming & Multitasking → S26–S28
  - Interrupts → S29–S32
  - OS Operations & Dual Mode → S33–S36
  - Kernel Data Structures → S37–S39

### B · Content redesign (Ch1 template)
- Rewrite `src/content/chapters/01-introduction.mdx` into notes format:
  section leads (one-line essence) → tight bullets, broken-out definitions, bolded key terms,
  comparison **tables** for every distinction, cited throughout.
- Keep both widgets (`StorageHierarchy`, `InterruptCycle`), the 4 figures, the callouts.
- New MDX helpers: `Lead.astro` (section essence), `SlideRef.astro`, definition-list styling.

### C · Visual elevation ("shadcn look", same stack)
- Tune `tokens.css`/`global.css`: keep slate palette, add notes tokens (lead, citation pill, table),
  refine borders/elevation/type rhythm.
- Polish components: Callout, Figure, code frames, Sidebar, BottomNav, home cards, chapter chrome,
  tables, definition lists. Zero new JS, zero Tailwind. shadcn MCP = design reference only.

### D · Figures
- Redraw the 4 Ch1 SVGs (`system-structure`, `computer-organization`, `memory-address-space`,
  `mode-transition`) at higher fidelity — cleaner type, clearer structure, new palette,
  theme-aware (CSS vars/`currentColor`), accessible (`role=img`, `viewBox`, titled).

### E · Exam mode (after A–D)
- `CONTENT/Chapter 1 Review Questions.pdf` → `src/content/questions/ch01.json` (schema exists).
- One Svelte island `QuizRunner` (`client:only="svelte"`, localStorage progress): pick chapter/topic
  → answer mcq/true-false/numeric/short → instant feedback with worked solution + slide citation
  → running score, resumable. Replaces `/exam` stub. Mobile-first, one island, no backend.

### F · Remove GSD + session continuity
- Strip "GSD Workflow Enforcement" + "Developer Profile" from `OS-web/CLAUDE.md`.
- Continuity via auto-loading project memory (`MEMORY.md` + notes) and a git-tracked
  `docs/HANDOFF.md`.

## Conventions
- Atomic commits, plain messages, **zero AI attribution**.
- Blank line between logic lines (JS/TS/Svelte); verify in-browser before claiming done.
